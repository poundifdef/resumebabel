${contact.fname} ${contact.lname}

${contact.street}
${contact.city}, ${contact.state} ${contact.zip}
${contact.email}
${contact.phone}

OBJECTIVE

${objective}

WORK EXPERIENCE
{% for key, job in enumerate(experience) %}
${job.name} (${job.start_date} - ${job.end_date})
${job.title}

${job.description} 
{% end %}
EDUCATION
{% for key, school in enumerate(education) %}
${school.name} (${education[key].start_date} - ${education[key].end_date})
${school.degree}

${school.description}
{% end %}
